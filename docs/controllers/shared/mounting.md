---
sidebar_position: 5.5
description: "Wall-mount, DIN-rail, and the shared mounting footprint for every current IgorBox controller"
---

# Mounting

Every current-generation IgorBox controller shares the same chassis and base plate, so one mounting story covers all of them: the [Output 8 MKII](/docs/controllers/output-8-mkii/overview), [Input 16](/docs/controllers/input-16/overview), [LED Controller](/docs/controllers/led-controller/overview), and [IgorBox DMX](/docs/controllers/dmx/overview).

The chassis has holes for wall-mount directly with screws through the side flanges, and we sell a DIN-rail bracket kit that can use the side flange holes to position the controller in 5 different positions.

## Footprint

The base plate, viewed from the top down. Dimensions are in millimeters [brackets] and inches:

![Top-down mounting footprint of the IgorBox base plate, shared by all current-generation controllers](/img/IgorBox-Base-Dimensions.png)

## Upgrading from earlier hardware

- **From an Output 8 MKI**: the MKII hole pattern matches the MKI, so your existing rack-mount, wall-mount, and DIN-rail hardware is reusable.
- **From an Input 8**: the Input 16 uses the same chassis dimensions, so existing brackets and racking are reusable.
