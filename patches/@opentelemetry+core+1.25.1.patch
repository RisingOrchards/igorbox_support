diff --git a/node_modules/@opentelemetry/core/build/esm/baggage/propagation/W3CBaggagePropagator.js b/node_modules/@opentelemetry/core/build/esm/baggage/propagation/W3CBaggagePropagator.js
index 3beba8d..c635faa 100644
--- a/node_modules/@opentelemetry/core/build/esm/baggage/propagation/W3CBaggagePropagator.js
+++ b/node_modules/@opentelemetry/core/build/esm/baggage/propagation/W3CBaggagePropagator.js
@@ -42,27 +42,40 @@ var W3CBaggagePropagator = /** @class */ (function () {
     };
     W3CBaggagePropagator.prototype.extract = function (context, carrier, getter) {
         var headerValue = getter.get(carrier, BAGGAGE_HEADER);
-        var baggageString = Array.isArray(headerValue)
-            ? headerValue.join(BAGGAGE_ITEMS_SEPARATOR)
-            : headerValue;
-        if (!baggageString)
+        if (!headerValue)
             return context;
         var baggage = {};
-        if (baggageString.length === 0) {
-            return context;
-        }
-        var pairs = baggageString.split(BAGGAGE_ITEMS_SEPARATOR);
-        pairs.forEach(function (entry) {
-            var keyPair = parsePairKeyValue(entry);
-            if (keyPair) {
-                var baggageEntry = { value: keyPair.value };
-                if (keyPair.metadata) {
-                    baggageEntry.metadata = keyPair.metadata;
+        var count = 0;
+        var totalSize = 0;
+        var headerValues = Array.isArray(headerValue) ? headerValue : [headerValue];
+        headers: for (var i = 0; i < headerValues.length; i++) {
+            var baggageString = headerValues[i];
+            var start = 0;
+            while (start < baggageString.length && count < BAGGAGE_MAX_NAME_VALUE_PAIRS) {
+                var end = baggageString.indexOf(BAGGAGE_ITEMS_SEPARATOR, start);
+                var entryEnd = end === -1 ? baggageString.length : end;
+                var entryLength = entryEnd - start;
+                if (entryLength <= BAGGAGE_MAX_PER_NAME_VALUE_PAIRS) {
+                    var keyPair = parsePairKeyValue(baggageString.substring(start, entryEnd));
+                    if (keyPair) {
+                        var entrySize = (count === 0 ? 0 : 1) + entryLength;
+                        if (totalSize + entrySize > BAGGAGE_MAX_TOTAL_LENGTH)
+                            break headers;
+                        var baggageEntry = { value: keyPair.value };
+                        if (keyPair.metadata) {
+                            baggageEntry.metadata = keyPair.metadata;
+                        }
+                        baggage[keyPair.key] = baggageEntry;
+                        count++;
+                        totalSize += entrySize;
+                    }
                 }
-                baggage[keyPair.key] = baggageEntry;
+                if (end === -1)
+                    break;
+                start = end + 1;
             }
-        });
-        if (Object.entries(baggage).length === 0) {
+        }
+        if (count === 0) {
             return context;
         }
         return propagation.setBaggage(context, propagation.createBaggage(baggage));
diff --git a/node_modules/@opentelemetry/core/build/esnext/baggage/propagation/W3CBaggagePropagator.js b/node_modules/@opentelemetry/core/build/esnext/baggage/propagation/W3CBaggagePropagator.js
index bd17a61..b21b43b 100644
--- a/node_modules/@opentelemetry/core/build/esnext/baggage/propagation/W3CBaggagePropagator.js
+++ b/node_modules/@opentelemetry/core/build/esnext/baggage/propagation/W3CBaggagePropagator.js
@@ -40,27 +40,39 @@ export class W3CBaggagePropagator {
     }
     extract(context, carrier, getter) {
         const headerValue = getter.get(carrier, BAGGAGE_HEADER);
-        const baggageString = Array.isArray(headerValue)
-            ? headerValue.join(BAGGAGE_ITEMS_SEPARATOR)
-            : headerValue;
-        if (!baggageString)
+        if (!headerValue)
             return context;
         const baggage = {};
-        if (baggageString.length === 0) {
-            return context;
-        }
-        const pairs = baggageString.split(BAGGAGE_ITEMS_SEPARATOR);
-        pairs.forEach(entry => {
-            const keyPair = parsePairKeyValue(entry);
-            if (keyPair) {
-                const baggageEntry = { value: keyPair.value };
-                if (keyPair.metadata) {
-                    baggageEntry.metadata = keyPair.metadata;
+        let count = 0;
+        let totalSize = 0;
+        const headerValues = Array.isArray(headerValue) ? headerValue : [headerValue];
+        headers: for (const baggageString of headerValues) {
+            let start = 0;
+            while (start < baggageString.length && count < BAGGAGE_MAX_NAME_VALUE_PAIRS) {
+                const end = baggageString.indexOf(BAGGAGE_ITEMS_SEPARATOR, start);
+                const entryEnd = end === -1 ? baggageString.length : end;
+                const entryLength = entryEnd - start;
+                if (entryLength <= BAGGAGE_MAX_PER_NAME_VALUE_PAIRS) {
+                    const keyPair = parsePairKeyValue(baggageString.substring(start, entryEnd));
+                    if (keyPair) {
+                        const entrySize = (count === 0 ? 0 : 1) + entryLength;
+                        if (totalSize + entrySize > BAGGAGE_MAX_TOTAL_LENGTH)
+                            break headers;
+                        const baggageEntry = { value: keyPair.value };
+                        if (keyPair.metadata) {
+                            baggageEntry.metadata = keyPair.metadata;
+                        }
+                        baggage[keyPair.key] = baggageEntry;
+                        count++;
+                        totalSize += entrySize;
+                    }
                 }
-                baggage[keyPair.key] = baggageEntry;
+                if (end === -1)
+                    break;
+                start = end + 1;
             }
-        });
-        if (Object.entries(baggage).length === 0) {
+        }
+        if (count === 0) {
             return context;
         }
         return propagation.setBaggage(context, propagation.createBaggage(baggage));
diff --git a/node_modules/@opentelemetry/core/build/src/baggage/propagation/W3CBaggagePropagator.js b/node_modules/@opentelemetry/core/build/src/baggage/propagation/W3CBaggagePropagator.js
index d295950..33889e3 100644
--- a/node_modules/@opentelemetry/core/build/src/baggage/propagation/W3CBaggagePropagator.js
+++ b/node_modules/@opentelemetry/core/build/src/baggage/propagation/W3CBaggagePropagator.js
@@ -43,27 +43,39 @@ class W3CBaggagePropagator {
     }
     extract(context, carrier, getter) {
         const headerValue = getter.get(carrier, constants_1.BAGGAGE_HEADER);
-        const baggageString = Array.isArray(headerValue)
-            ? headerValue.join(constants_1.BAGGAGE_ITEMS_SEPARATOR)
-            : headerValue;
-        if (!baggageString)
+        if (!headerValue)
             return context;
         const baggage = {};
-        if (baggageString.length === 0) {
-            return context;
-        }
-        const pairs = baggageString.split(constants_1.BAGGAGE_ITEMS_SEPARATOR);
-        pairs.forEach(entry => {
-            const keyPair = (0, utils_1.parsePairKeyValue)(entry);
-            if (keyPair) {
-                const baggageEntry = { value: keyPair.value };
-                if (keyPair.metadata) {
-                    baggageEntry.metadata = keyPair.metadata;
+        let count = 0;
+        let totalSize = 0;
+        const headerValues = Array.isArray(headerValue) ? headerValue : [headerValue];
+        headers: for (const baggageString of headerValues) {
+            let start = 0;
+            while (start < baggageString.length && count < constants_1.BAGGAGE_MAX_NAME_VALUE_PAIRS) {
+                const end = baggageString.indexOf(constants_1.BAGGAGE_ITEMS_SEPARATOR, start);
+                const entryEnd = end === -1 ? baggageString.length : end;
+                const entryLength = entryEnd - start;
+                if (entryLength <= constants_1.BAGGAGE_MAX_PER_NAME_VALUE_PAIRS) {
+                    const keyPair = (0, utils_1.parsePairKeyValue)(baggageString.substring(start, entryEnd));
+                    if (keyPair) {
+                        const entrySize = (count === 0 ? 0 : 1) + entryLength;
+                        if (totalSize + entrySize > constants_1.BAGGAGE_MAX_TOTAL_LENGTH)
+                            break headers;
+                        const baggageEntry = { value: keyPair.value };
+                        if (keyPair.metadata) {
+                            baggageEntry.metadata = keyPair.metadata;
+                        }
+                        baggage[keyPair.key] = baggageEntry;
+                        count++;
+                        totalSize += entrySize;
+                    }
                 }
-                baggage[keyPair.key] = baggageEntry;
+                if (end === -1)
+                    break;
+                start = end + 1;
             }
-        });
-        if (Object.entries(baggage).length === 0) {
+        }
+        if (count === 0) {
             return context;
         }
         return api_1.propagation.setBaggage(context, api_1.propagation.createBaggage(baggage));
